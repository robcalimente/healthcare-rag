.env
.DS_Store
__pycache__/
*.pyc
.venv/
venv/

# Raw Synthea output and the generator tool itself — large, regenerable from
# backend/scripts/prepare_data.py + backend/scripts/build_index.py, not committed.
# data/index/ and data/processed/clinical.db ARE committed (via Git LFS, see
# .gitattributes) -- the deployed backend needs them at runtime and there's no
# build step in production that can regenerate them from Synthea.
data/synthea_output/
data/processed/chunks.jsonl
synthea/
data/*.log

node_modules/
dist/
frontend/.env
